/* export function movieDataIsValid({ title, year, genre, yStart, yEnd }) {
  if (
    title === '' ||
    (genre === '') | (year === '') | !(parseInt(year) >= yStart) | !(parseInt(year) <= yEnd)
  )
    return {
      success: false,
      errorMsg: `* Title, Year and Genre is required, Year must be between ${yStart} and ${yEnd} `
    }

  return { success: true }
}
 */
